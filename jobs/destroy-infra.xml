<?xml version='1.1' encoding='UTF-8'?>
<flow-definition plugin="workflow-job@2.40">
  <description>Destroy Infra Job</description>
  <keepDependencies>false</keepDependencies>

  <properties>
    <hudson.model.ParametersDefinitionProperty>
      <parameterDefinitions>

        <!-- ACTION dropdown -->
        <hudson.model.ChoiceParameterDefinition>
          <name>CONFIRM_DESTROY</name>
          <description>
            YES = Run Terraform DESTROY PLAN (preview only)  
            NO  = Run Terraform DESTROY APPLY (will destroy resources)
          </description>
          <choices class="java.util.Arrays$ArrayList">
            <a class="string-array">
              <string>YES</string>
              <string>NO</string>
            </a>
          </choices>
        </hudson.model.ChoiceParameterDefinition>

        <!-- ENV dropdown -->
        <hudson.model.ChoiceParameterDefinition>
          <name>ENV</name>
          <description>Select environment</description>
          <choices class="java.util.Arrays$ArrayList">
            <a class="string-array">
              <string>dev</string>
              <string>prod</string>
            </a>
          </choices>
        </hudson.model.ChoiceParameterDefinition>

      </parameterDefinitions>
    </hudson.model.ParametersDefinitionProperty>
  </properties>

  <definition class="org.jenkinsci.plugins.workflow.cps.CpsFlowDefinition">
    <script><![CDATA[
pipeline {
  agent any

  stages {
    stage('Destroy Infra') {
      steps {
        script {
          withCredentials([[$class: 'AmazonWebServicesCredentialsBinding', credentialsId: 'aws-creds']]) {

            sh "cd labs/${params.ENV} && terraform init"

            if (params.CONFIRM_DESTROY == 'YES') {
              sh "cd labs/${params.ENV} && terraform plan -destroy"
            } else {
              sh "cd labs/${params.ENV} && terraform destroy -auto-approve"
            }

          }
        }
      }
    }
  }
}
    ]]></script>
    <sandbox>true</sandbox>
  </definition>

  <triggers/>
  <disabled>false</disabled>
</flow-definition>
